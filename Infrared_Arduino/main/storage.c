/*
 * storage.c
 *
 * Created: 6/16/2017 8:35:23 PM
 *  Author: Rchan
 */ 

//void ShiftToLED(){
	//if (BUTTON_A == HIGH){
		////Serial.print(BUTTON_A);
				//
		//// Increment counter to visualize current count
		//general_use_counter = general_use_counter + 1;
				//
		//// Move cursor to first row first column
		////lcd.setCursor(0, 0);
				//
		//// Print counter number on LCD
		////lcd.print(general_use_counter);
				//
		//// Storage register is positive-edge triggered. Lower it to prepare the data
		//digitalWrite(RCLK_PIN, LOW);
				//
		//// Shift all bits to their shift register
		//shiftOut(SER_PIN, SRCLK_PIN, MSBFIRST, general_use_counter);
				//
		//// Storage register is positive-edge triggered. Turn it on to store data into storage registrer
		//digitalWrite(RCLK_PIN, HIGH);
		//} else {
		////lcd.print("0");
		////Serial.print(BUTTON_A);
	//};
//}